target/
*.class
*.log
.idea/
*.iml
.vscode/
.DS_Store
design/canvas/jrsctl-screens.html
/dist/build/
/dist/out/
*.jfr
hs_err_pid*
.flattened-pom.xml

# per-developer agent tooling: plugin lists, permissions, worktrees (never shared)
.claude/

# live-server scratch home used for manual checks against a real install (never shared)
/tmp-real-home/

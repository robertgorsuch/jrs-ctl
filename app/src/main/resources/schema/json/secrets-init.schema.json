{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jaspersoft.com/jrsctl/secrets-init.schema.json",
  "title": "jrsctl secrets init --json (spec §5.2)",
  "type": "object",
  "additionalProperties": false,
  "required": ["file"],
  "properties": {
    "file": { "type": "string", "description": "Path of the secrets.enc that was created." }
  }
}

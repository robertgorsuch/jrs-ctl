{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jaspersoft.com/jrsctl/api-launch-token.schema.json",
  "title": "POST /api/auth/launch (spec §11.2): the bearer token for a single-use launch code",
  "type": "object",
  "additionalProperties": false,
  "required": ["token"],
  "properties": {
    "token": { "type": "string", "minLength": 1 }
  }
}

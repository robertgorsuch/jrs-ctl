{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jaspersoft.com/jrsctl/console.schema.json",
  "title": "jrsctl console --json (spec §13): the one place the per-launch token URL is printed",
  "type": "object",
  "additionalProperties": false,
  "required": ["url"],
  "properties": {
    "url": { "type": "string", "format": "uri" }
  }
}
